runs:
  using: "composite"
  steps:
    - run: |
        echo ". ${{ github.action_path }}/helpers.sh" >> ~/.bash_profile
        cat ~/.bash_profile
        cat ${{ github.action_path }}/helpers.sh
        . ${{ github.action_path }}/helpers.sh
      shell: bash
